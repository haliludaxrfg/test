#include <bits/stdc++.h>
using namespace std;
/*
int main(){
    vector <int> nums;
    int _;
    while (1)
    {
        cin >> _;
        if(_==0)
            break;
        else
            nums.push_back(_);
    }
    
    if(nums.empty())
        printf("NULL");
    else{
        for(auto &a:nums){
            cout << a <<" ";
        }
    }
    
}*///用vector秒了，真的是招笑了
struct ListNode
{
    int val;
    struct ListNode* next;
};

void insert(ListNode *cur,ListNode *toadd){
    ListNode
}

